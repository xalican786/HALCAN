[build]
builder = "NIXPACKS"

[deploy]
startCommand            = "node --max-old-space-size=400 --expose-gc src/index.js"
healthcheckPath         = "/ping"
healthcheckTimeout      = 300
restartPolicyType       = "ON_FAILURE"
restartPolicyMaxRetries = 10
